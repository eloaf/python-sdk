from setuptools import setup

setup(
    name="mcp",
    version="1.6.0",  # Define the version here
    description="Your project description",
    packages=["mcp"],  # Replace with your actual package list
)
